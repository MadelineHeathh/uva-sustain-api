# UVA Sustainability Metrics API - Environment Variables Template
# Copy this file to .env and update with your values (if needed)
# Note: This app doesn't require any secrets - all values have safe defaults

# Port the application will run on (default: 8080)
PORT=8080

# Host address the application will bind to (default: 0.0.0.0)
# Use 0.0.0.0 to accept connections from any network interface
HOST=0.0.0.0

# Optional: Path to the data file (default: assets/uva_energy_data_template.csv)
# Only needed if you want to use a different data file
# DATA_FILE=assets/uva_energy_data_template.csv

# Optional: For Azure App Service deployments
# WEBSITES_PORT=8080

# Optional: Flask application path (for Flask CLI)
# FLASK_APP=src/app.py
